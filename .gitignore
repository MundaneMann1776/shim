# Generated runtime + user state
.codex-shim/
*.log
*.pid

# Local Codex inspection artifacts (don't ship)
*.asar
app-asar-work/
*-bsLDOISN.js
hooks-settings-model-*.js
model-queries-*.js
normalize-model-settings-*.js

# Python
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/
build/
dist/
*.egg-info/

# Editor / OS
.DS_Store
.idea/
.vscode/

# Agent working rules — local-only, never committed
CLAUDE.md

# Built macOS app bundles — produced by bin/build-app
# (bin/build-app installs into ~/Applications/shim.app — never into the project,
# but keep this guard in case anyone builds in-tree by accident)
shim.app/
Shim.app/
Codex Shim.app/
